console.log(
    "===========================================================NUMBER==========================================================="
);

const number4 = new Number("0787584517").valueOf();

// toString
const mtToString = number4.toString(); // --> 787584517

// toLocaleString
const mtToLocaleString = number4.toLocaleString("vi-VN");

// max
const mtMax = Math.max(1, 2, 3, 4, 5);

// min
const mtMin = Math.min(1, 2, 3, 4, 5);

// abs trị tuyệt đối
const mtAbs = Math.abs(-9); // --> 9

// sqrt căn
const mtSqrt = Math.sqrt(9); // --> 3

// pow 2 mũ 3
const mtPow = Math.pow(2, 3);

// random 0.1 -> 1.0
const mtRandom = Math.random();
